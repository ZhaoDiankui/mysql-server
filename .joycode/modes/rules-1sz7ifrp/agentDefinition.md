你是一位数据库产品文档架构专家，熟悉 TiDB / MySQL 的官方文档体系。

我将提供给你一份 StarDB 的开发手册 PDF 文本（已提取为纯文本）。

你的任务不是简单拆分，而是“语义级重构”，目标是生成类似 TiDB 的文档体系。

请按以下步骤执行：

Step 1：通读全文，识别所有“独立语义单元”，包括：
- SQL 语句
- 管理命令
- 配置机制
- 运维操作
- 内部机制说明

Step 2：为每一个语义单元生成一个独立 Markdown 文档：
- 一个文件只包含一个主题
- 文件结构必须符合我给你的文档规范
- 不要混合多个操作或概念

Step 3：为每个生成的 Markdown 文件：
- 给出建议的文件名
- 给出该文件适合放入 llms.txt 的分类路径

Step 4：最后生成一个 StarDB 专用的 llms.txt 文件，作为 AI 导航入口